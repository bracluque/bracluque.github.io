# How to run — sr-extraction-pipeline (generic)

**Status: placeholder.** Will be adapted from the original ESD `HOW_TO_RUN.txt` — same three-stage flow (Reviewer A → Reviewer B → Reconciler, each in a fresh Claude Code session), same skip-logic and parallel-wave defaults, but written against the generic `template/` files instead of ESD's.

TODO (next task):
- [ ] Generalize the step-by-step setup (project root / input / output folder instructions)
- [ ] Keep the "why this works" explanation of orchestrator + subagent context management — unchanged, it's already generic
- [ ] Keep the common-issues Q&A — unchanged, it's already generic
- [ ] Add a step 0.5: "write your codebook" pointing at `template/SKILL_codebook.md`
